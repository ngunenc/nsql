# Proje kökü (opsiyonel): .env'in bulunduğu dizin; belirtilmezse otomatik aranır
# NSQL_PROJECT_ROOT=/path/to/your/app

# Veritabanı — new nsql() varsayılan olarak bu değerleri kullanır
DB_HOST=localhost
DB_PORT=3306
DB_NAME=database_name
DB_USER=database_user
DB_PASS=database_password
DB_CHARSET=utf8mb4
DB_DRIVER=mysql

# Önbellek Ayarları
QUERY_CACHE_ENABLED=true
QUERY_CACHE_TIMEOUT=300
QUERY_CACHE_SIZE_LIMIT=1000

# Connection Pool Ayarları
DB_MIN_CONNECTIONS=2
DB_MAX_CONNECTIONS=10
DB_HEALTH_CHECK_INTERVAL=60
DB_CONNECTION_TIMEOUT=30
DB_READ_WRITE_SPLIT=false
DB_POOL_LOG_FILE=pool_log.txt

# Debug modu (true/false)
DEBUG_MODE=false

# Loglama ayarları
LOG_FILE=error_log.txt
STATEMENT_CACHE_LIMIT=100

# Şifreleme (production'da zorunlu önerilir)
# Üretmek için: php -r "echo base64_encode(random_bytes(32)), PHP_EOL;"
# Tanımlı değilse geliştirmede storage/keys/encryption.key otomatik oluşur (git'e eklemeyin)
# ENCRYPTION_KEY=
